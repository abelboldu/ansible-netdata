---
# defaults file for leanbit.netdata
netdata_install_dir: /opt
netdata_systemd: True
netdata_port: 80
netdata_deps:
  - zlib1g-dev
  - uuid-dev
  - libmnl-dev
  - gcc
  - make
  - git
  - autoconf
  - autoconf-archive
  - autogen
  - automake
  - pkg-config
netdata_version: v1.5.0
